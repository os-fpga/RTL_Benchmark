ABS_PATH_CONFIG_SS := $(dir $(realpath $(lastword $(MAKEFILE_LIST))))

include $(ABS_PATH_CONFIG_SS)/../ip/atcdmac300/config.mk
CONFIG_SS_RTL_SRC := $(RTL_SRC)
CONFIG_SS_RTL_INC := $(RTL_INC)
CONFIG_SS_MODEL_SRC := $(MODEL_SRC)

include $(ABS_PATH_CONFIG_SS)/../ip/atcgpio100/config.mk
CONFIG_SS_RTL_SRC += $(RTL_SRC)
CONFIG_SS_RTL_INC += $(RTL_INC)
CONFIG_SS_MODEL_SRC += $(MODEL_SRC)

include $(ABS_PATH_CONFIG_SS)/../ip/atciic100/config.mk
CONFIG_SS_RTL_SRC += $(RTL_SRC)
CONFIG_SS_RTL_INC += $(RTL_INC)
CONFIG_SS_MODEL_SRC += $(MODEL_SRC)

include $(ABS_PATH_CONFIG_SS)/../ip/atcpit100/config.mk
CONFIG_SS_RTL_SRC += $(RTL_SRC)
CONFIG_SS_RTL_INC += $(RTL_INC)
CONFIG_SS_MODEL_SRC += $(MODEL_SRC)

include $(ABS_PATH_CONFIG_SS)/../ip/atcspi200/config.mk
CONFIG_SS_RTL_SRC += $(RTL_SRC)
CONFIG_SS_RTL_INC += $(RTL_INC)
CONFIG_SS_MODEL_SRC += $(MODEL_SRC)

include $(ABS_PATH_CONFIG_SS)/../ip/atcuart100/config.mk
CONFIG_SS_RTL_SRC += $(RTL_SRC)
CONFIG_SS_RTL_INC += $(RTL_INC)
CONFIG_SS_MODEL_SRC += $(MODEL_SRC)

include $(ABS_PATH_CONFIG_SS)/../ip/atcwdt200/config.mk
CONFIG_SS_RTL_SRC += $(RTL_SRC)
CONFIG_SS_RTL_INC += $(RTL_INC)
CONFIG_SS_MODEL_SRC += $(MODEL_SRC)

include $(ABS_PATH_CONFIG_SS)../ip/pvt_mon/config.mk
ifeq ($(USE_PVT_STUB),1)
	CONFIG_SS_RTL_SRC += $(RTL_STUB)
else
	CONFIG_SS_RTL_SRC += $(RTL_SRC)
endif
CONFIG_SS_RTL_INC += $(RTL_INC)
CONFIG_SS_MODEL_SRC += $(MODEL_SRC)

include $(ABS_PATH_CONFIG_SS)/system_control_unit/config.mk
CONFIG_SS_RTL_SRC += $(RTL_SRC)
CONFIG_SS_RTL_INC += $(RTL_INC)
CONFIG_SS_MODEL_SRC += $(MODEL_SRC)

include $(ABS_PATH_CONFIG_SS)../ip/GbE/config.mk
ifeq ($(USE_GBE_STUB),1)
	CONFIG_SS_RTL_SRC += $(RTL_STUB)
else
	CONFIG_SS_RTL_SRC += $(RTL_SRC)
endif
CONFIG_SS_RTL_INC += $(RTL_INC)
CONFIG_SS_MODEL_SRC += $(MODEL_SRC)
CONFIG_SS_LIB_SRC := $(LIB_SRC)
CONFIG_SS_LEF_SRC := $(LEF_SRC)

include $(ABS_PATH_CONFIG_SS)../ip/USB/config.mk
ifeq ($(USE_USB_STUB),1)
	CONFIG_SS_RTL_SRC += $(RTL_STUB)
else
	CONFIG_SS_RTL_SRC += $(RTL_SRC)
endif
CONFIG_SS_RTL_INC += $(RTL_INC)
CONFIG_SS_MODEL_SRC += $(MODEL_SRC)
CONFIG_SS_LIB_SRC += $(LIB_SRC)
CONFIG_SS_LEF_SRC += $(LEF_SRC)

ifeq ($(USE_BCPU_ENCRYPTED),1)
	include $(ABS_PATH_CONFIG_SS)/../ip/Andes_N22/encrypted/config.mk
else
	include $(ABS_PATH_CONFIG_SS)/../ip/Andes_N22/config.mk
endif
ifeq ($(USE_BCPU_STUB),1)
	CONFIG_SS_RTL_SRC += $(RTL_STUB)
else
	CONFIG_SS_RTL_SRC += $(RTL_SRC)
endif
CONFIG_SS_RTL_INC += $(RTL_INC)
CONFIG_SS_LIB_SRC += $(LIB_SRC)
CONFIG_SS_LEF_SRC += $(LEF_SRC)
CONFIG_SS_MODEL_SRC += $(MODEL_SRC)


include $(ABS_PATH_CONFIG_SS)/../ip/mailbox/config.mk
CONFIG_SS_RTL_SRC += $(RTL_SRC)
CONFIG_SS_RTL_INC += $(RTL_INC)
CONFIG_SS_MODEL_SRC += $(MODEL_SRC)

include $(ABS_PATH_CONFIG_SS)/../ip/PUFCC/config.mk
ifeq ($(USE_PUFCC_STUB),1)
	CONFIG_SS_RTL_SRC += $(RTL_STUB)
else
	CONFIG_SS_RTL_SRC += $(RTL_SRC)	
endif

CONFIG_SS_RTL_INC += $(RTL_INC)
CONFIG_SS_LIB_SRC += $(LIB_SRC)
CONFIG_SS_LEF_SRC += $(LEF_SRC)
CONFIG_SS_MODEL_SRC += $(MODEL_SRC)

include $(ABS_PATH_CONFIG_SS)/../ip/PLLTS16FFCFRACF/config.mk
CONFIG_SS_RTL_SRC += $(RTL_SRC)
CONFIG_SS_RTL_INC += $(RTL_INC)
CONFIG_SS_LIB_SRC += $(LIB_SRC)
CONFIG_SS_LEF_SRC += $(LEF_SRC)
CONFIG_SS_MODEL_SRC += $(MODEL_SRC)

CONFIG_SS_RTL_SRC += $(ABS_PATH_CONFIG_SS)/rs16.v
CONFIG_SS_RTL_SRC += $(ABS_PATH_CONFIG_SS)/pads.v
CONFIG_SS_RTL_SRC += $(ABS_PATH_CONFIG_SS)/pad_shell.v
CONFIG_SS_RTL_SRC += $(ABS_PATH_CONFIG_SS)/soc_config_subsystem.sv

RTL_SRC := $(CONFIG_SS_RTL_SRC)
RTL_INC := $(CONFIG_SS_RTL_INC)
MODEL_SRC := $(CONFIG_SS_MODEL_SRC)
LIB_SRC := $(CONFIG_SS_LIB_SRC)
LEF_SRC := $(CONFIG_SS_LEF_SRC)

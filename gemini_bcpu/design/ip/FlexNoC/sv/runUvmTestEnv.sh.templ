source anvu_sim_options.sh
solverOpts="+ntb_enable_solver_trace_on_failure=${enableSolverTraceOnFailure} +ntb_enable_solver_trace=0 +ntp_solver_mode=1"

export LD_LIBRARY_PATH="sv:noc:${LD_LIBRARY_PATH}:${FLEXNOC_LD_LIBRARY_PATH}:${FLEXNOC_EXTERNAL_LD_LIBRARY_PATH}"
export XVMTESTENV_PERF="${2}".perf.csv

echo "set WAVES ${2}.waves.vpd" > "${2}".tcl
cat  anvu_sim_waves.tcl        >> "${2}".tcl
echo "quit"                     > "${2}".cmd

./runUvmTestEnv -cm_name "${1}" +UVM_TESTNAME="${1}" +vcs+lic+wait ${userSimFlags} ${solverOpts} ${waveOpts} +anvu_log_file="${2}".anvu.log +anvu_txt_file_dir=txt +anvu_test_options="${3}" +anvu_test_power_domains="${4}" &> "${2}".log < "${2}".cmd

#exit ${?}

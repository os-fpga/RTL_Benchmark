 
# Yosys synthesis script for sync_sdp_write_first
# Read source files 
verific -vlog-libext .v
verific -vlog-libext .sv
verific -vlog-define 
verific -vlog2k ${ROOT_PATH}/RTL_Benchmark/Verilog/yosys_validation/bram_tests/sync_sdp_write_first/sync_sdp_write_first.sv
verific -import sync_sdp_write_first


# Technology mapping
hierarchy -top sync_sdp_write_first


plugin -i synth-rs

synth_rs  -tech ${ARCHITECTURE} -top sync_sdp_write_first -de -goal delay -carry no        -de_max_threads -1   

write_verilog -noexpr -nodec ${TOP}_post_synth.v
write_blif sync_sdp_write_first_post_synth.blif

  
